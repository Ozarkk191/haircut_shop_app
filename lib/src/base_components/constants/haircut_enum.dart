enum HomeLayoutPage {
  DASHBOARD,
  PROMOTION,
  WALLET_REPORT,
  MARKETPLACE,
  ACCOUNT
}
enum DiscountType { PERCENT, PRICE, FREE }
enum UserGender { MALE, FEMALE }
enum ServiceType { BOOKING, DELIVERY }
enum PaymentOptions { WALLET, CARD }
enum RatingMode { single, fraction }
enum ResetPasswordMethod { PHONE, EMAIL }
